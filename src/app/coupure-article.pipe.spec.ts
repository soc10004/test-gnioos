import { CoupureArticlePipe } from './coupure-article.pipe';

describe('CoupureArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new CoupureArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
